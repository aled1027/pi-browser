# Pi-browser



TODO

- Move the tutor plugins to be in the tutors example and have the tutor example load them in at browser page load time.
- Sync agent state, like the virtual file system, to IndexedDB and local storage (whichever is appropropriate which information, so that we can resume sessions)